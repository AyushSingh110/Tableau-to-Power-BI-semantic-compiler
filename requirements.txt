# Runtime dependencies (pinned to tested versions).
# Tableau Hyper extract access (official API)
tableauhyperapi==0.0.24081
# Data profiling & relationship inference
pandas==2.2.3
numpy==2.1.3
# Tableau TWB XML parsing (optional faster backend; stdlib ElementTree is used)
lxml==5.3.0
# IR schema validation (used from Phase 2 onward)
jsonschema==4.23.0
